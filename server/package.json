{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:bio": "tsx src/scripts/generate-bio.ts",
    "build": "pnpm run generate:bio && tsc -p tsconfig.json && node esbuild.config.mjs",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.112.3",
    "@aws-sdk/client-dynamodb": "^3.1090.0",
    "@aws-sdk/client-sesv2": "^3.1090.0",
    "@aws-sdk/client-s3": "^3.1090.0",
    "@aws-sdk/lib-dynamodb": "^3.1090.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.162",
    "@types/node": "^26.1.1",
    "aws-sdk-client-mock": "^4.1.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
