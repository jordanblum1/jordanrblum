<nav class="wide" aria-label="Primary">
  <a class="wordmark serif" href="/" aria-label="Jordan Blum — home">Jordan Blum</a>
  <div class="links mono-meta">
    <a href="#work">work</a>
    <a href="#projects">things</a>
    <a href="#about">about</a>
    <a class="email" href="mailto:jordanblum16@gmail.com">email ↗</a>
  </div>
</nav>

<style>
  nav {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--space-6);
    padding-block: var(--space-6);
  }

  .wordmark {
    color: var(--heading);
    font-size: 1.25rem;
    line-height: 1;
  }

  .links {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: var(--space-6);
  }

  .links a {
    transition: color var(--duration-default) var(--ease);
  }

  html.pointer-moved .links a:hover {
    color: var(--accent);
  }

  .email {
    color: var(--heading);
  }

  @media (max-width: 35rem) {
    .links {
      gap: var(--space-4);
    }

    .links a:nth-child(3) {
      display: none;
    }
  }
</style>
